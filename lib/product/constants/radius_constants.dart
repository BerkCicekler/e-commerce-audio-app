import 'package:flutter/rendering.dart';

final class RadiusConstants {
  RadiusConstants._();

  /// This radius is used in every form field widget
  static final formInputCircularRadius = BorderRadius.circular(10);

  static final smallBorderRadius = BorderRadius.circular(10);

  static final mediumBorderRadius = BorderRadius.circular(15);

  static final largeBorderRadius = BorderRadius.circular(30);

  static final largeRadius = Radius.circular(30);
}
